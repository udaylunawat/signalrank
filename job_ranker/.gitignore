# Data files — too large for git
*.csv
outputs/
*.parquet
*.db
*.duckdb
*.duckdb.wal
duckdb
duckdb.wal

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Env — never commit real credentials
.env
# Allow the template/copy
!.env\ copy
!job_ranker/.env\ copy

# Logs
*.log

# Allow job tracker (user data, manually curated)
!users/example/job_tracker.csv
!users/example/job_tracker_backup.csv

# Mini ranker cache
.mini_ranker_cache/

# OS
.DS_Store
uv.lock
duckdb copy
